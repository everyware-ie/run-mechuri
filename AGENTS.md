# AGENTS.md

**이 저장소에서 AI 에이전트가 작업할 때 지키는 규칙이다. 도구를 가리지 않는다.**

작업자마다 쓰는 모델이 다르다. **누가 어떤 도구로 하든 같은 흐름이 나오도록** 여기에 모아 둔다. 이 파일이 워크플로우의 정본이고, [CLAUDE.md](CLAUDE.md)에는 배경과 더 긴 설명이 있다.

## 이 저장소가 무엇인가

러닝 기록을 그림으로 만들어 인스타그램 스토리에 올리는 iOS 앱이다. React Native + Expo로 만들고 Swift 브릿지 셋(HealthKit, AVFoundation, 인스타 공유)을 쓴다.

**기획 문서와 코드가 한 저장소에 있다.** 그래서 둘이 어긋나면 같은 PR에서 함께 고치는 것이 기본이다.

---

## 1. 작업 흐름

### 1-1. 아이디어에서 결정까지

```
개인 노트 → 회의 안건 → 회의 종합 → 결정문 → PRD·FRD
```

| 단계 | 어디에 |
|---|---|
| 개인 노트 | `idea/<닉네임>` 브랜치, `docs/ideation/<닉네임>/` |
| 회의 안건 | `docs/ideation/<닉네임>/<날짜>-meeting-agenda.md` |
| 회의 종합 | `docs/meetings/<날짜>/synthesis.md` |
| 결정문 | `docs/decisions/<날짜>-<주제>.md` + `index.md`에 한 줄 |

**개인 노트는 고치지 않는다.** 생각이 바뀌면 새 노트로 보완한다. 남의 폴더는 읽기 전용이다.

**안건은 회의 전에 공유한다.** 공유는 미리 하고 회의는 논의에 쓰기로 했다.

**결정이 제품 정의를 바꾸면 PRD를, 화면과 기능의 규칙을 바꾸면 FRD를 같은 PR에서 고친다.**

### 1-2. 결정에서 구현까지

```
브랜치 → 구현 노트 → 구현 → 문서 동기화 → PR → 머지
```

1. **브랜치를 판다.** `<작업자>/<작업 유형>/<기능>` 형식이다
2. **구현 노트를 만든다.** `docs/product/features/<기능>.md`에 FRD 링크를 넣는다. **이게 없으면 훅이 편집을 막는다**
3. **근거가 되는 FRD를 사용자에게 보여주고 확인받는다.** 이 단계는 건너뛸 수 없다
4. **구현한다**
5. **어긋난 것을 고친다.** FRD와 다른 점을 발견하면 구현 노트의 "어긋남 기록"에 적고, FRD도 같은 PR에서 고친다
6. **커밋 전에 확인한다**
7. **PR을 만든다.** 참조하는 FRD가 `approved`가 아니면 훅이 막는다

커밋 전 확인은 이 셋이다.

```bash
node_modules/.bin/tsc --noEmit          # 타입
node_modules/.bin/eslint src/ modules/  # 린트
python3 scripts/check-docs.py           # 문서 (docs/를 건드렸을 때)
```

### 1-3. 배포

명령과 주의사항은 [빌드·개발 명령어 모음](docs/ops/build-and-dev-commands.md)에 있다. 실기기 확인부터 TestFlight 제출까지 실제로 쓰는 것만 적혀 있다.

**배포하면 그 기록을 같은 문서에 남긴다.** 빌드 번호, PR, 제출 결과, 확인한 것.

### 1-4. QA

내부 테스트에서 나온 것은 [QA 기록](docs/ops/qa-log.md)에 모은다. 카톡으로 오가는 것이 흘러가 버리기 때문이다.

```
제보 → QA 기록 → (정할 것이면) 회의 → 결정 → FRD 개정 → 구현
```

**바로 고칠 수 있는 것과 정해야 하는 것을 갈라 적는다.** 무엇이 아직 안 정해졌는지가 보여야 한다.

### 1-5. 탐색은 이 흐름 밖이다

**실험, 프로토타입, 일회성 스크립트는 위 절차를 따르지 않아도 된다.** 배경 이미지를 만든 `scripts/generate-backgrounds.py`가 그렇게 나왔다.

**제품 코드로 들어가는 시점부터 규칙이 적용된다.** 탐색으로 얻은 결론이 제품을 바꾸면 그때 PRD나 FRD에 올린다.

---

## 2. 기능의 무엇과 왜는 FRD가 정한다

- `docs/specs/frd/`가 유일한 정본이다. **`status: approved`인 것만 구현 근거가 된다**
- `docs/specs/prd/`는 제품 요구 문서다. **PRD 없이 제품 기능을 만들기 시작하지 않는다**
- 코드가 FRD와 다르면 코드를 고친다
- **디자인 시안과 FRD가 부딪치면 FRD를 따른다.** 시안은 좋은 제안이지만 팀 승인을 거치지 않았다

## 3. 하류를 고치다 상류가 낡으면 같이 고친다

```
결정문 → PRD → FRD → 구현 노트
```

FRD를 고치다 PRD가 낡으면 **같은 PR에서** 함께 고친다. 나중에 몰아서 하면 어느 쪽이 맞는지 알 수 없게 된다.

**팀이 정한 것을 개인이 문서에서 뒤집지 않는다.** 결정문과 어긋나면 결정문에 개정을 덧붙인다.

**새 용어를 만들거나 뜻을 바꾸면 [용어집](.claude/domain/glossary.md)부터 고친다.**

절 번호로 참조할 때는 **제목도 함께 적는다.** "§7"이 아니라 "§7 결과물의 형태". 절이 하나 끼어들면 번호만으로는 조용히 어긋난다.

## 4. 문서는 사람이 읽는 글로 쓴다

**전체를 다 읽은 사람만 이해할 수 있는 글은 안 된다.** 처음 보는 사람이 그 문단만 읽어도 무슨 말인지 알아야 한다.

- **긴 줄표(`—`)를 쓰지 않는다.** `-`, `( )`, `:`로 바꾼다
- **외부 자료를 근거로 썼으면 원문 링크를 단다.** 못 찾으면 못 찾았다고 적는다. 찾은 척하지 않는다
- **모르는 것은 `[확인 필요]`로 남긴다.** 추측으로 메우지 않는다
- **누가 무슨 말을 했는지 적을 때는 실제로 한 말만 적는다.** 짐작한 것은 짐작이라고 밝힌다

**줄 수를 줄이는 것이 목표가 아니다.** 내용을 쉽게 파악할 수 있게 하는 것이 목표다.

자세한 규칙과 예시는 [문서 작성 컨벤션](.claude/conventions/writing.md)에 있다.

## 5. 브랜치와 커밋

- 형식은 `<작업자>/<작업 유형>/<기능>`이다. 예: `phs00/fix/record-list`
- 작업 유형은 `feature`, `fix`, `chore`, `docs`다
- **`main`에 직접 push 하지 않는다**

자세한 것은 [Git 컨벤션](.claude/conventions/git.md)에 있다.

## 6. 훅이 두 곳에서 막는다

| 언제 | 무엇이 필요한가 |
|---|---|
| 구현 소스를 편집할 때 | 이 브랜치에 `docs/product/features/<기능>.md` 확인 노트가 있어야 한다 |
| PR을 만들 때 | PR이 참조하는 FRD의 `status`가 `approved`여야 한다 |

**"바로 진행"으로도 건너뛸 수 없다.**

브랜치 유형이 `chore`·`docs`면 첫 번째는 면제다. 테스트 파일도 대상이 아니다.

설치는 `sh scripts/hooks/install.sh`다.

## 7. 시작하기 전에 확인한다

**`.claude/` 아래의 규칙 문서를 읽었는지 스스로 확인하고 시작한다.**

| | |
|---|---|
| [.claude/conventions/writing.md](.claude/conventions/writing.md) | 문서를 쓰거나 고칠 때 |
| [.claude/conventions/git.md](.claude/conventions/git.md) | 브랜치를 파거나 커밋할 때 |
| [.claude/domain/glossary.md](.claude/domain/glossary.md) | 용어를 쓰거나 새로 만들 때 |

**읽지 않았으면 먼저 읽는다.** 이 파일의 요약만으로 대신하지 않는다.

## 8. 확인하고 진행한다

**무엇을 어떻게 할지 먼저 알리고 확인을 받은 뒤에 진행한다.** 임의로 앞서 나가지 않는다.

## 9. 규칙이 안 맞아 보이면

**규칙을 지키느라 나쁜 해법을 고르지 않는다.**

FRD대로 만들었더니 불편하거나 이상하면, 여기 적힌 절차가 이 상황에 안 맞으면, **이유를 적어 제안한다.** 바꾸지 말자는 것이 아니라 **조용히 갈라지지 말자**는 것이다.

**규칙마다 왜 그런지가 적혀 있다.** 이유를 알면 그 규칙이 안 맞는 상황도 알아볼 수 있다.

---

## 이 파일을 고칠 때

**워크플로우와 규칙의 정본은 이 파일이다.** [CLAUDE.md](CLAUDE.md)에는 배경과 더 긴 설명이 있고, 자세한 규칙은 `.claude/conventions/`에 있다. **어느 쪽을 고치든 다른 쪽도 같이 본다.**

다른 도구를 쓰게 되면 그 도구가 찾는 파일에서 이 파일을 가리키기만 하면 된다.
