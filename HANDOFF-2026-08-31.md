# 인계 노트 (2026-08-31, 사무실 → 집)

> 이 파일은 프로젝트 정본 문서가 아니다. "집 클로드"가 이어서 작업할 수 있도록
> 이 세션에서만 오간, 레포에 안 남아 있는 맥락을 적어둔 임시 노트다. 흡수되면
> 지워도 된다. **먼저 이 파일 전체를 읽고 시작할 것.**

## 0. 지금 뭘 하고 있었나 (한 줄)

디자인 목업(3안, "1a 야간 네온" 전체 화면 목업)을 받고, **"목업 전체 구현(Strava 제외)"**
으로 스코프를 확정해 6단계 계획을 세우고 순서대로 구현 중. 1~2단계 완료, 3~6단계 남음.

## 1. 레포 상태 — 반드시 먼저 확인할 것

```
git checkout JiEung2/feature/result-editing
git pull
```

**⚠️ 이 브랜치는 아직 `main`에 안 들어갔다.** 스택 구조가 이렇다:

```
main
 └─ JiEung2/chore/expo-project-setup   (PR #31 → main, 머지됨)
     └─ JiEung2/feature/healthkit-bridge   (PR #32, 머지됨)
         └─ JiEung2/feature/route-renderer   (PR #33, 머지됨)
             └─ JiEung2/feature/core-loop-screens   (PR #34, 머지됨)
                 └─ JiEung2/feature/result-editing   (PR #35, 머지됨) ← 지금 여기 + 커밋 2개 더
```

PR #32~35는 서로에게 순차적으로(다음 브랜치로) 머지된 스택형 PR이고, **이 스택 전체를
main으로 보내는 PR은 아직 없다.** main에는 아직 Expo 스캐폴딩(#31)만 있다. 지금 작업
중인 커밋 2개(다듬기 슬라이더까지)는 `result-editing` 브랜치 위에 그대로 푸시해뒀다.
집에서도 같은 브랜치에 계속 커밋하면 된다 — 새 브랜치 만들 필요 없음.

main으로의 최종 통합 PR은 아직 언제 낼지 정해진 게 없다. 사용자가 "이제 올려" 라고
하기 전엔 만들지 말 것.

## 2. 지금까지 한 일 (전체, 이번 목업 구현 이전 포함)

- HealthKit 브릿지(Swift, `frontend/modules/health-kit-bridge`) — 러닝 기록·경로 좌표 읽기
- AVFoundation 경로 렌더러(Swift, `frontend/modules/route-renderer`) — 프레임 단위로 그려
  `AVAssetWriter`로 mp4 인코딩. 실시간 캡처는 2026-08-16 스파이크에서 배경 합성과 함께
  무너지는 걸 확인하고 이 방식으로 전환한 것(route-rendering FRD 참고)
- 코어 루프 화면 4개: 홈 → 기록 선택 → 배경 선택 → 편집 → 공유 (Expo Router,
  `frontend/src/app/`)
- 디자인 시스템 "1a 야간 네온" 전 화면 적용 (§3 참고)
- **목업 구현 1~2단계** (§4 참고)

## 3. 디자인 시스템 — "1a 야간 네온"

`frontend/src/constants/theme.ts`가 정본. 새 화면·컴포넌트는 반드시 이 토큰을 쓴다.

```ts
Colors.bg = '#0B0D10'         // 캔버스 배경
Colors.bgCard = '#14181D'
Colors.text = '#EDF1F5'
Colors.textMuted = '#7C8894'
Colors.accent = '#FF5A2B'     // UI 크롬 강조색(버튼·토글·선택 상태)
Colors.lineWarm = '#FFF3EC'   // 경로 드로잉 전용 — accent와 다른 색임에 주의
Fonts.sans = 'SpaceGrotesk_500Medium' / sansBold = 'SpaceGrotesk_700Bold'
Fonts.mono = 'JetBrainsMono_500Medium'
```

**주의**: 경로 드로잉 자체의 팔레트(`#FFF3EC` 따뜻한 흰색 + `#FF6B4A` 주황 글로우)는
UI 크롬의 accent(`#FF5A2B`)와 다른, 더 오래된 목업(`docs/ideation/JiEung2/2026-08-04-route-overlay-mockup.html`)
에서 온 것이고, 의도적으로 분리돼 있다. 섞지 말 것 — `route-preview.tsx`·
`route-thumbnail.tsx`·Swift 렌더러가 이미 이렇게 구현돼 있으니 그대로 따라가면 됨.

## 4. 목업 구현 6단계 계획 — 상태

| # | 내용 | 상태 | 커밋 |
|---|---|---|---|
| 1 | 보관함 썸네일·이어서 만들기·결과물 상세 | ✅ 완료 | `08a5459` |
| 2 | 편집 화면 정밀 슬라이더(다듬기 세기·모서리) | ✅ 완료 | `97286d4` |
| 3 | 각인 편집 | ✅ 완료 | (아래 참고) |
| 4 | 인코딩 진행률 + 취소 | ✅ 완료 | (아래 참고) |
| 5 | 공유 카드(캡션 등) | ⬜ 다음 차례 | — |
| 6 | 설정 화면 | ⬜ | — |

각 단계 완료 시 `docs/product/features/<기능>.md`의 "구현 노트"를 갱신하고, 커밋 메시지에
`(목업 구현 N/6)`을 붙이는 걸 관례로 삼았다. 계속 그렇게 할 것.

### 4-1. 방금 끝낸 2단계 요약 (참고용)

`frontend/src/lib/route-smoothing.ts`(TS)와 `RouteRendererModule.swift`(Swift) 양쪽에
같은 파이프라인(중복 제거 → 이동평균 → RDP 단순화 → 모서리 라운딩)을 이식. 적용 순서를
`projectPoints → applySmoothing → applyTransform`으로 두 언어에서 맞췄다(그룹 변형보다
먼저 적용해야 확대해도 슬라이더 의미가 화면상에서 안 바뀜). 자세한 설계 이유는
`docs/product/features/result-editing.md`의 "다듬기 세기 (§5)" 절에 다 적어뒀다.

### 4-2. 3단계(각인 편집) 완료 요약

`docs/product/features/result-editing.md`의 "각인 편집 (§7)" 절에 설계 이유 다 적어뒀다.
핵심만: 각인 넷은 위치 하나짜리 묶음, `edit.tsx`에 드로잉/각인 대상 선택 토글 추가,
표시 모드 3종 + 항목별 켜고 끄기, TS(`route-preview.tsx` StampLayer)·Swift(`drawStamps`)
양쪽에 같은 레이아웃 수식 이식. **알아둘 것 두 가지**:

- **페이스·심박은 근사다.** `RunRecord`가 평균값만 갖고 있어(시계열 HealthKit 샘플을
  안 읽어옴) "그 구간 값"·"그 시점 bpm"을 진행 내내 평균값 고정으로 대체했다. 나중에
  시계열을 붙이면 없앨 수 있는 근사
- **§4-1 "화면에서 직접 탭해서도 고를 수 있게 한다"를 토글 UI로만 만족시켰다.** 드로잉/
  각인을 캔버스에서 직접 탭해 전환하는 히트테스트는 아직 없음 — 다음에 붙일 후보

### 4-3. 4단계(인코딩 진행률 + 취소) 완료 요약

`docs/product/features/export-and-share.md`의 "인코딩 진행률 + 취소 (§2-3, 목업 구현
4/6)" 절에 설계 이유 다 적어뒀다. 핵심만: common-rules §6 대기 표시 타이밍(0.3초 뒤 표시,
최소 0.5초 유지, 2초 넘으면 진행률+취소)을 `share.tsx`에 그대로 구현. Swift
`Events("onRenderProgress")` + `sendEvent`로 6프레임마다 진행률을 보내고, JS는
`RouteRenderer.addListener(...)`로 구독. 취소는 인스턴스 플래그(`isCancelled`) 하나로 —
이 앱은 한 번에 렌더 하나만 돈다는 전제. 취소·실패 둘 다 `router.back()`으로 편집 화면에
돌아가고, `edit.tsx`가 계속 저장해둔 초안 덕에 편집값이 자동으로 유지된다(F1·F2).

### 4-4. ⚠️ 원본 디자인 목업 HTML을 잃어버렸다

이번 6단계 계획의 근거가 된 "3안" 전체 화면 목업(S0~S9, 각인 편집·인코딩 진행률·공유
카드·설정 화면까지 다 그려져 있던 것)은 **채팅에 붙여넣기로만 존재했고 레포 파일로
저장한 적이 없다.** 지금 이 요약 자체도 그 목업을 요약한 요약이라, 3~6단계를 만들 때
목업의 정확한 여백·문구·인터랙션 디테일을 그대로 재현할 방법이 없다.

**할 수 있는 것**: FRD 텍스트(정본, `docs/specs/frd/`)와 이미 적용된 디자인 시스템
토큰(§3)으로 새로 설계해서 만드는 것. 지금까지도 실제로 이렇게 해왔다(1~2단계 다 FRD
문구 기준으로 만들고, 색·레이어만 목업에서 가져옴).

**사용자에게 물어볼 것**: 그 목업을 다시 붙여넣어 줄 수 있으면(원래 어디서 만들었는지에
따라 다시 꺼낼 수 있을 수도 있음) 3~6단계 디테일이 훨씬 정확해진다. 없으면 FRD +
디자인 시스템만으로 계속 진행하면 된다 — 지금까지 방식과 다르지 않다.

### 4-5. 실기기 확인 중 발견한 것 — 불빛 러너 목업 어긋남 (정정 완료)

2단계까지 실기기 빌드로 확인하다가, "불빛 러너" 프리셋이 `2026-08-04-route-overlay-mockup.html`
(이건 남아있는 파일)과 다르다는 걸 발견해서 정정했다(`c591157`). "지나온 길" 레이어에
목업이 가진 옅은 글로우가 빠져 있었고, "최근 잔광" 길이가 목업(짧고 스치는 느낌)보다
훨씬 길게(거리 기준 10%) 잡혀 있어서 6%로 줄였다. **다른 프리셋도 실기기에서 목업과
나란히 비교해볼 가치가 있다** — 시뮬레이터/웹 미리보기로는 글로우 필터가 잘 안 보여서
이런 어긋남을 놓치기 쉽다.

### 4-6. 5단계(공유 카드) 시작 전 알아둘 것

- FRD 대상: `docs/specs/frd/export-and-share.md`(§3 인스타그램 공유는 4단계-인스타
  브릿지 이후라 아직 스코프 밖 — "공유 카드"는 목업에 있던 캡션 편집 등 §4 기기 저장
  화면 언저리의 UI로 추정. **§4-4에서 언급한 대로 원본 목업을 다시 못 받으면, FRD 문구
  기준으로 범위를 좁혀서 설계할 것**)
- 지금 `share.tsx`의 "완성!" 화면은 결과 경로 텍스트 + "기기에 저장" + "홈으로" 뿐이다

## 5. 애플 서명 / 기기 테스트 — 이번 세션에 직접 확인·수정함

이전 세션 요약을 그대로 옮겼던 부분을 이번 세션에 실제로 빌드해보면서 검증하고 고쳤다.
**아래는 재확인이 아니라 확정된 사실이다.**

- jminkkk(Jimin Kim)의 Apple Developer 계정이 **Individual(개인) 타입**이라 팀 단위로
  인증서·프로비저닝 프로파일을 공유하는 기능이 없다. 그래서 jminkkk가 `.p12`(서명
  인증서)와 `.mobileprovision`(프로비저닝 프로파일, 이름 `Mechuri App Dev`, 팀
  `BT2KSYYPDF`)을 수동으로 발급해서 넘겨줬고, JiEung2가 로컬 Xcode에서 Manual Signing으로
  붙이는 방식이다 — 인증서는 `Apple Development: Jimin Kim (2FQNL3F4R5)`
- `frontend/ios/`, `*.p12`, `*.mobileprovision`은 전부 **gitignore돼 있어 이 레포에 없다.**
  `frontend/ios/`는 `npx expo prebuild`로 로컬에서 만든 것 — 로컬 네이티브 모듈
  (HealthKit·RouteRenderer)이 있어서 Expo Go로는 안 되고 development build가 필요함
- **`npx expo run:ios`(터미널)로 빌드하면 안 된다.** 이 CLI가 실행될 때마다 이 Mac의
  Xcode에 로그인된 (전혀 다른, JiEung2 개인) Apple ID로 자동 서명을 시도하면서
  `CODE_SIGN_STYLE`을 Automatic으로 되돌리고, `PROVISIONING_PROFILE_SPECIFIER`("Mechuri
  App Dev")는 그대로 남겨서 매번 "conflicting provisioning settings" 빌드 에러가 난다
  (이번 세션에 두 번 재현 확인). **반드시 Xcode GUI(`open frontend/ios/frontend.xcworkspace`
  → ▶️ Run)로 빌드할 것.** GUI는 프로젝트 파일 값을 그대로 쓰고 CLI처럼 덮어쓰지 않는다.
  Xcode의 Signing & Capabilities 탭에서 "Automatically manage signing"이 켜져 있으면
  꺼야 하고, Team=Jimin Kim(BT2KSYYPDF)·Profile=Mechuri App Dev로 맞춰야 한다
- **Xcode Run은 네이티브 빌드+설치만 한다. JS 서버(Metro)는 따로 켜야 한다** —
  `frontend/`에서 `npm start`(`expo start`). Swift 코드가 안 바뀌었으면 Xcode Run을 다시
  누를 필요 없이 Metro만 켜두면 JS/TS 수정이 Fast Refresh로 바로 반영된다
- **집 컴퓨터에서 기기 빌드를 하려면**: (a) `.p12`·`.mobileprovision` 파일 자체를
  git이 아닌 다른 경로(에어드랍, USB, 클라우드)로 옮겨오거나, (b) jminkkk에게 새로
  발급을 부탁해야 한다. `.p12`는 개인키를 포함한 파일이라 기기에 안 묶여 있고
  그대로 다른 맥에 옮겨서 Keychain에 import하면 재사용 가능함(revoke만 안 됐다면).
  프로파일은 `~/Library/Developer/Xcode/UserData/Provisioning Profiles/`에 설치돼
  있어야 Xcode가 인식한다(더블클릭하면 거기 설치됨)
- 번들 ID는 `com.mechuri.runmechuri` (`frontend/app.json`), EAS 프로젝트는 이미 연결돼
  있음(`eas.json` 존재, development/preview/production 빌드 프로필 있음)

## 6. 확인 명령 (커밋 전 항상 돌릴 것)

```bash
cd frontend
npx tsc --noEmit          # 타입체크
npx expo lint              # 린트 (2026-08-31에 처음 설정됨, eslint.config.js)
cd ..
python3 scripts/check-docs.py   # 문서 정합성(링크·frontmatter·FRD derives_from)
```

라우트를 새로 추가했으면 `.expo/types/router.d.ts` 재생성이 필요할 수 있다(`timeout`
명령이 이 zsh 환경엔 없어서 `npx expo start --offline & PID=$!; sleep 8; kill $PID`
패턴으로 백그라운드 실행 후 죽이는 방식을 씀).

## 7. 기타 참고

- FRD 정본: `docs/specs/frd/` (approved만 구현 근거)
- 구현 노트: `docs/product/features/` — FRD 링크 + 코드 구조. 단계 끝낼 때마다 갱신
- git 훅 이미 로컬 설치돼 있음(`sh scripts/hooks/install.sh`로 재설치 가능) — 구현 소스
  편집 시 이 브랜치에 구현 노트가 있어야 편집 허용, PR 생성 시 참조 FRD가 approved인지 검사
- 이 파일 자체는 정본이 아니므로, 흡수되면(예: 이 노트의 내용이 각 기능 구현 노트·이
  세션의 대화로 넘어가면) 지워도 된다. 지우는 커밋은 `chore:`로.
