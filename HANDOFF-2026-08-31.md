# 인계 노트 (2026-08-31, 갱신본)

> 이 파일은 프로젝트 정본이 아니다. "다음 클로드"가 이어서 작업할 수 있도록, 이 세션에서만
> 오간 맥락을 적어둔 임시 노트다. 흡수되면 지워도 된다(`chore:` 커밋). **먼저 전체를 읽고 시작.**

## 0. 지금 상태 (한 줄)

"3안" 야간 네온 시안을 화면에 반영하는 중. 홈·편집·배경·인코딩·완성 화면을 시안대로 다시
짰고, 경로 렌더를 react-native-svg → **react-native-skia**로 갈아끼웠다. **네이티브 재빌드가
안 돼서 폰에서 아직 확인 못 함** (§2 참고).

## 1. 레포 상태 — 반드시 먼저 확인

```
git checkout JiEung2/feature/share-card
git pull
```

- **브랜치**: `JiEung2/feature/share-card` (= `core-loop-screens` 위 + 이번 작업)
- **PR #39** 열려 있음 → `main`. **PR #32~#37 전부 대체한다** (스택이 서로에게만 머지돼 있고 main엔
  아직 Expo 스캐폴딩 #31만 있음). #39 하나만 리뷰·머지하면 됨. #37은 닫아도 됨
- pre-pr-checklist 훅 통과함 (참조 FRD 전부 approved)

이번 세션 커밋 2개:
- `feat(ui): "3안" 시안 반영 — 화면 셸·Skia 경로 렌더러·공유 카드`
- `fix(renderer): 인코딩 중 OOM(jetsam) — 프레임마다 autoreleasepool + 배경 1회 스케일`

## 2. ⚠️ 지금 막힌 것 — 네이티브 재빌드

이번에 **네이티브가 두 군데** 바뀌었다:
1. `@shopify/react-native-skia@2.6.2` 추가 (route-preview·route-thumbnail이 이걸 씀)
2. `RouteRendererModule.swift` OOM 수정

**JS 리로드로는 안 된다.** 폰에서 `RNSkiaModule could not be found` 에러가 뜨는 이유.

```bash
cd frontend
npm install
npx expo prebuild -p ios        # ios/Podfile 에 skia + pod install
# "이미 있음"으로 넘어가면: npx expo prebuild -p ios --clean (서명 초기화됨)
open ios/*.xcworkspace
```
그다음 **Xcode ▶️ Run으로 실제 재빌드** + 별도 터미널 `npm start`.

확인: `grep -i skia ios/Podfile.lock` 에 `react-native-skia` 있어야 함.

## 3. 이 머신 (새 Mac) — Apple 서명

이전 세션 노트에서 확인된 사실:
- jminkkk(Jimin Kim) Apple Developer 계정이 **Individual**이라 팀 공유가 안 됨. `.p12`(서명 인증서,
  `Apple Development: Jimin Kim (2FQNL3F4R5)`)와 `.mobileprovision`(프로파일 `Mechuri App Dev`,
  팀 `BT2KSYYPDF`)을 jminkkk가 수동 발급해 넘겨줬다
- 이 새 Mac에 `.p12`는 더블클릭으로 Keychain에 넣었음. `.mobileprovision`도 **더블클릭**하면
  `~/Library/Developer/Xcode/UserData/Provisioning Profiles/` 에 설치됨(화면 반응 없음)
- Xcode → 타겟 Signing & Capabilities → **Automatically manage signing 끄기** → Team: Jimin Kim
  (BT2KSYYPDF) → Profile: `Mechuri App Dev`. 인증서는 `Apple Development: Jimin Kim (2FQNL3F4R5)`
- **또는 그냥 빨리 볼 거면**: Xcode GUI에서 자동 서명 + 본인 Apple ID (7일 개인 빌드). §5가 경고한
  건 `npx expo run:ios`(CLI)지 GUI 자동 서명은 괜찮음
- `frontend/ios/`·`*.p12`·`*.mobileprovision`은 전부 gitignore — 레포에 없음
- 번들 ID `com.mechuri.runmechuri`, EAS 프로젝트 연결됨(`eas.json`, dev/preview/production 프로필)
- `npx expo run:ios`(CLI) 쓰지 말 것 — 이 Mac의 다른 Apple ID로 자동 서명 시도하며 매번
  "conflicting provisioning settings" 에러. **반드시 Xcode GUI ▶️**
- release 빌드로 성능 보려면: Xcode → Edit Scheme → Run → Build Configuration Debug→**Release** → ▶️
  (JS가 묶여 빌드됨, Metro 불필요, 대신 코드 고칠 때마다 재빌드)

## 4. 이번 세션에 한 것

### 4-1. 시안 반영 (화면 레이아웃)

- **화면 셸**: `_layout.tsx`에서 네이티브 내비 바 제거(`headerShown: false`). 새 컴포넌트
  `src/components/screen-header.tsx` — 화면마다 `<ScreenHeader title onBack right/>` (← + 가운데 제목)
- **홈 S1** (`index.tsx`): "메추리 / ARCHIVE · N" 헤더 + 최근 결과물 히어로 카드(하단 오버레이 거리 +
  ▶) + "내가 만든 것" 2열 그리드 + 점선 빈 상태. (시안 하단 탭은 2026-08-25 결정으로 빠짐 → "새로
  만들기" 버튼이 대신)
- **편집 S7** (`edit.tsx`): 미리보기 카드 + 프리셋 3칩(accent 채움) + SMOOTH/CORNER 슬라이더 2축
  **직접 노출** + [각인]·[배경 바꾸기]·[초기화] 아웃라인 행. **각인 편집은 하단 시트(S6)**로 분리 —
  시트 열려 있는 동안만 미리보기에서 각인 끌어 이동(`editTargetRef`). 드로잉/각인 토글·"고급 설정"
  토글 제거
- **배경 S5** (`background-selection.tsx`): 배경+경로 합성 큰 카드(좌상단 "9:16 · 가로 채움") + 기본
  이미지 가로 스와치 3 + "갤러리" 점선 슬롯(비활성) + 헤더 "다음"
- **인코딩 S8a** (`share.tsx` progress 상태): "내보내기" 헤더 + 카드(경로 + 어두운 오버레이 + % +
  "영상으로 만드는 중") + 얇은 진행 바 + "12초 · 1080×1920" + 취소 + 안내
- **완성 S8b** (`share.tsx` done 상태): 공유 카드("메추리 · 날짜" 태그 + 큰 거리 + 각인)

### 4-2. 경로 렌더: react-native-svg → react-native-skia

- **왜**: 시안 경로 그림은 `<canvas>` 2D(`shadowBlur` 네온 글로우). SVG `FeGaussianBlur`로 못 맞췄고,
  프레임마다 SVG `<Path>` 재렌더가 렉이었다(dev 빌드 특히)
- **어떻게**: `src/components/route-preview.tsx`·`route-thumbnail.tsx` 새로 작성. Skia로 시안 `paint()`
  레이어 이식 — `<Shadow dx=0 dy=0 blur=r color=#FF5A2B>` 글로우, 옅은 원본 · 지나온 길(+글로우) ·
  최근 6% 잔광(강한 글로우) · 머리 점 · 구간 반짝임. 부분 경로는 기존 `pointsUpToDistance` 슬라이스를
  `Skia.Path.MakeFromSVGString`으로
- **각인 텍스트는 SVG 오버레이 유지** — 로드된 JetBrains Mono를 또렷하게. Skia `<Canvas>` 위에 절대
  위치 `<Svg>`. `StampLayer`는 `StampLayerSvg`로 이름 바뀌고 alias export
- 팔레트: 선 `#FFF3EC`, 글로우 `#FF5A2B`, 옅은 원본 `rgba(237,241,245,.13)`, base `rgba(237,241,245,.2)`

### 4-3. Swift 렌더러 OOM 수정

`RouteRendererModule.swift` `writeClip`:
- 프레임 루프 본문을 `autoreleasepool { }`로 감쌈 (없으면 360프레임 × 수십 MB가 안 풀려 jetsam이 앱을
  code 9로 죽임 — "영상 만들기"에서 발생하던 그 크래시)
- 배경을 `prepareBackground`로 출력 크기(1080×1920) 1회 크롭·스케일 후 재사용
- 취소 플래그를 `isReadyForMoreMediaData` 대기 루프에서도 확인
- **아직 실기기 검증 안 됨** — 재빌드 후 "영상 만들기"가 끝까지 도는지 확인 필요

## 5. 디자인 시스템 — "1a 야간 네온" / "3안"

`frontend/src/constants/theme.ts`가 정본. 새 화면·컴포넌트는 이 토큰만 참조.

```ts
Colors.bg = '#0B0D10'  bgCard = '#14181D'  text = '#EDF1F5'  textMuted = '#7C8894'
Colors.accent = '#FF5A2B'  accentText = '#0B0D10'  border/borderStrong
Colors.lineWarm = '#FFF3EC'   // 경로 드로잉 전용 — accent와 다름
Fonts.sans = SpaceGrotesk_500Medium / sansBold = _700Bold / mono = JetBrainsMono_500Medium
Radius.pill 26 / card 22 / chip 19
```

시안 원본: `docs/ideation/JiEung2/2026-08-31-mvp-mockup-3an.md` (12개 화면 구성·문구를 텍스트로
남긴 것 — 원본 `.dc.html`은 폰트 1.5MB라 레포에 없음). 경로 드로잉 팔레트는 이와 별개로 더 오래된
`docs/ideation/JiEung2/2026-08-04-route-overlay-mockup.html` 기준(route-preview.tsx 주석 참고).

## 6. 남은 일

### 6-1. 목업 구현 6단계 기준

| # | 단계 | 상태 |
|---|---|---|
| 1~5 | 홈·편집·각인·인코딩·공유 카드 | ✅ (시안 반영까지) |
| 6 | **설정 화면 (S9)** | ❌ 미착수 — approved FRD에 없음, phs00 확인 대기 |

### 6-2. FRD 기준 큰 구멍

- **인스타 스토리 공유 (export-and-share §3)** — ❌ 미착수. **MVP 필수**. 브릿지 3건 중 마지막
  (Sharing to Stories). Facebook App ID 필요(`[확인 필요]`, 스토어 제출물 트랙)
- **실제 배경 이미지 3장** (background-selection §2-2) — jminkkk/phs00. 지금은 단색 자리표시자
- **갤러리에서 배경 고르기 + 사진 권한** (background-selection §3, common-rules §1) — 슬롯만 있음
- **Swift 렌더러 글로우를 Skia 미리보기에 맞추기** — 미리보기(Skia)와 최종 mp4(CoreGraphics)의
  글로우가 아직 다름. `route-rendering.md` 어긋남 기록에 있음. 실기기 확인 후
- **3주차 실기기 품질**: route-rendering §10(선 굵기·대비), §7-5 안전영역 실측(상단 14%·하단 20%
  제안값), export-and-share §6-1 인코딩 소요 시간 실측
- result-editing §6 속도·색 조절 (여유 시)
- home-and-library §4 쇼케이스 (여유 시)

### 6-3. phs00 결정 대기 (스펙 없음 — CLAUDE.md 정합성 규칙상 구현 전 확인)

`docs/product/features/export-and-share.md`·`result-editing.md` 확인 노트에 다 적어둠:
1. **한 줄 문구(캡션)** — 시안 S6/S8b에 있는데 어느 FRD에도 없음. PRD §11이 텍스트-온-결과물
   후보를 약하게 봄. 결정되면 각인과 나란한 렌더 파라미터로 붙이면 됨(`RouteRendererModule`
   `caption` 필드 + `route-preview` CaptionLayer + `edit.tsx` 입력 + draft/results 저장)
2. **설정 화면 (S9)**
3. **result-editing §5 "다듬기는 기본 한 축"** ↔ 시안은 슬라이더 2개 — 시안대로 구현함. §5 개정 여부
4. **인코딩 실패 "다시 시도" 버튼 (S8a)** — 현재는 편집으로 돌아가 재시도(§2-4 F3)
5. 온보딩/연결 화면(S0·S2), 각인 "장소·날짜" 칩

## 7. 확인 명령 (커밋 전 항상)

```bash
cd frontend
node_modules/.bin/tsc --noEmit      # ⚠️ `npx tsc`는 엉뚱한 전역 tsc@2.0.4를 씀. 로컬 바이너리로.
node_modules/.bin/eslint src/       # 또는 npx expo lint
cd ..
python3 scripts/check-docs.py
```

**현재 lint 상태**: 에러 5건 — 전부 이 작업 이전부터 있던 것 (`slider.tsx` ×2, `edit.tsx`
panResponder ×2, `use-color-scheme.web.ts` ×1). 새 파일(route-preview·route-thumbnail·
screen-header)은 clean. tsc는 통과.

`node_modules`가 이 세션에서 여러 번 사라졌다 — cwd가 리셋되면서 착시였을 수 있음. `cd frontend`
확실히 하고 `npm install`.

## 8. 기타

- FRD 정본: `docs/specs/frd/` (approved만 구현 근거). 구현 노트: `docs/product/features/`
- git 훅: 구현 소스 편집 시 이 브랜치에 구현 노트 필요(있음), PR 생성 시 참조 FRD approved 검사(통과)
- 2026-08-25 결정: 하단 탭 안 씀, 홈 하나에 선형 루프
- reanimated 4 / worklets는 이미 deps에 있었음(Skia가 요구). babel 설정은 babel-preset-expo 자동 처리
