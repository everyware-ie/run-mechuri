# 홈과 보관함

- FRD: ../../specs/frd/home-and-library.md
- 이슈: -
- 구현 상태: 진행 중

## 구현 노트

`frontend/src/app/index.tsx`를 홈 화면으로 구현. §1-1 화면 구성 중 v0에 해당하는 부분만.

- **새로 만들기**(항상): `record-selection`으로 이동
- **결과물 목록**: `frontend/src/lib/results-store.ts`(AsyncStorage)에서 읽음. §2-1 정렬(러닝한 날 최신순) 반영
- **이어서 만들기**(§3): v0는 편집을 건너뛰므로 미완성 개념 자체가 없음 — 구현 안 함
- **쇼케이스**(§4): 여유 시 항목이라 v0 범위 밖

2026-08-25 결정("만들다 만 것은 가장 최근 1건만 보여준다")은 v0에서 해당 없음(미완성 자체가 없어서).

**아직 안 한 것**: 결과물 열기/다시 편집/삭제(§2-2), 복귀 위치(§5), 디자인 시스템 적용.

## 어긋남 기록

(아직 없음)
