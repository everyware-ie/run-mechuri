# 배경 선택

- FRD: ../../specs/frd/background-selection.md
- 이슈: -
- 구현 상태: 진행 중

## 구현 노트

`frontend/src/app/background-selection.tsx`. §1 MVP 기본 범위(기본 이미지)만 구현.

- **기본 이미지 3장이 정해졌다** (2026-09-01). `frontend/assets/backgrounds/`의 `아침-1-빛없음.png` · `점심-3-맑은한낮.png` · `저녁-3-맑은노을.png`다. 색값과 만든 방식은 [배경 이미지 노트](background-images.md)에 있다.
- **아직 코드에 안 붙었다.** 지금은 번들 스플래시 아이콘을 자리표시자로 쓰고 있고 화면에도 자리표시자라고 표시해뒀다. 파일을 이 셋으로 갈아끼우면 된다
- `expo-asset`으로 번들 이미지를 로컬 파일 경로로 변환해 렌더러 모듈에 넘김

**아직 안 한 것**: 갤러리에서 고르기·촬영(§3, 여유 시), 9:16 맞추기 확대·이동(§4-1), 영상 배경(§5).

## 어긋남 기록

(아직 없음)
