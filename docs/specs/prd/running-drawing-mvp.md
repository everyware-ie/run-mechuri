---
title: 러닝 드로잉 MVP 제품 요구 문서 (PRD)
product: running
type: prd
status: draft
updated: 2026-08-17
related: []
code_repo: https://github.com/everyware-ie/run-mechuri
---

# 러닝 드로잉 MVP 제품 요구 문서 (PRD)

> **status: `draft`.** 1주차(8/12~8/18) 조사가 끝나 §11-1~3에 담당자 추천안이 반영돼 있다. 8/18 회의에서 확정되면 `approved` 후보가 된다.
> **담당: phs00**
> 이 문서는 "무엇을, 왜"까지만 다룬다. 화면 단위 규칙과 수치는 FRD(`docs/specs/frd/`)로 내린다.

## 1. 한 줄 정의

뛴 경로를 러닝 영상 위에 선으로 살려서, 인스타그램 스토리에 올릴 수 있는 세로 클립으로 만들어 주는 제품.

| | |
|---|---|
| **목표(정성)** | 우리가 서비스를 써서 인스타 스토리에 처음 올렸을 때, 주변인에게 "우와" "이거 뭐야?" 소리를 1회 이상 듣기 |
| **출시일** | 2026-09-22(화). **"출시"를 무엇으로 볼지는 §11-1** (TestFlight 공개 링크가 추천안) |
| **검증 기간** | ~2026-09-29(화). 추석 9/24~27에 지인 영입을 노린다 |

목표가 정성적인 것은 여러 정량 후보(사용 횟수, 주 1회 이상 사용, 해피케이스 완주 등)를 검토한 끝에 내린 결론이다. 출처: 2026-08-11 통합 회의.

## 2. 배경

**이건 가설이 아니라 이미 일어나고 있는 행동이다.** 스트라바와 NRC 사용자들이 이미 경로 오버레이를 얹은 릴스를 올리고 있다. (JiEung2 2026-08-04)

**그런데 기존 공유 방식은 정적이다.** 러닝 앱이 합성해 주는 결과물은 경로와 페이스가 그냥 박힌 이미지뿐이다.

**우리는 자신의 성취를 공유하고 자랑하고 싶어하는 욕구를 채우는 서비스를 만든다.** 러닝 앱에 공유 기능을 덧붙이는 것이 아니라, 공유가 압도적으로 편하고 결과물이 살아 움직인다는 것을 제품의 중심에 둔다. 정확한 기록은 토대이지 차별점이 아니다. 차별점은 그 기록을 어떻게 보여주고 내보내느냐에 있다. (팀의 2026-07-14 원칙 - "기능은 뻔하더라도 성장과 경쟁을 우리만의 UX로" 반영)

**핵심 발견: 경로는 선이다.** GPS 트랙은 순서가 있는 좌표열이므로, 이미지가 아니라 선으로 다루면 시간축이 생긴다. 표현이 전부 여기서 갈라져 나온다. (JiEung2 2026-08-04)

## 3. 타겟 사용자

**팀 3명과 그 주변인.** 9/22 검증 대상을 그대로 타겟으로 삼는다.

범위를 좁게 잡은 것은 의도적이다. 목표가 "팀이 올렸을 때 주변인이 놀라는 것"이므로, 검증 대상과 타겟이 어긋나면 만들면서 판단 기준이 흔들린다. 일반 러너로의 확장은 검증 이후에 다시 본다. (2026-08-17 phs00 결정)

## 4. 제품 원칙

| # | 원칙 | 근거 |
|---|---|---|
| P1 | **경로와 기록을 표현하는 요소는 전부 러닝 데이터에서 나온다.** 예외는 배경 하나뿐이다(§6). 배경 위에 얹히는 것에 반짝이 파티클이나 예쁜 프레임을 넣기 시작하면 그냥 영상 편집 앱이 된다 | JiEung2 2026-08-04. 팀의 2026-07-14 차별화 원칙 반영 |
| P2 | **AI 이미지 생성은 현 단계에서 도입하지 않는다.** 따라서 렌더링 완성도가 "우와"를 만드는 유일한 레버다 | 2026-08-11 회의. 회의록의 "AI로 귀여운 이미지" 항목은 발산 중 나온 아이디어이며 도입 대상이 아니다 |
| P3 | **프리셋은 고르는 재미가 우선이다.** 코스 모양 기반 자동 추천은 초기값 역할이지 코어가 아니다 | 2026-08-17 phs00 결정. JiEung2 원안은 자동 추천을 코어로 봤으나 우선순위를 뒤집었다 |

## 5. MVP 범위

### 포함

1. **러닝 경로 데이터 확보** (확보 방식은 §11-2 미결)
2. **배경 위 경로 오버레이 렌더링** (배경은 §6 참고)
3. **인스타 스토리에 올릴 수 있는 파일로 내보내기**
4. **재방문 유인 하나** (무엇으로 할지는 §11-6 미결)

### 제외 (다음 라운드)

그룹 드로잉, 코스 공유와 네비게이션, 페이스 경쟁, 온라인 마라톤, 동기 모드(영상 촬영 시각과 GPS 시각 맞추기), 오버레이 위치·크기 드래그 편집, 영상 밝기에서 선 색 자동 추출.

출처: 2026-08-11 회의. 단 그룹 드로잉은 §10에서 다시 다룬다.

## 6. 사용자 플로우

### 코어 루프

```
[러닝 기록 선택] → [배경 선택] → [프리셋 선택] → [미리보기] → [내보내기] → [인스타 스토리]
```

**데이터 출처는 이 플로우에 나타나지 않는다.** GPX 업로드든, HealthKit 읽기든, 자체 트래킹이든 하나의 정규화된 트랙(시간순 좌표열 + 거리·시간·페이스 요약)으로 들어온 뒤에야 렌더러를 만난다. 이렇게 두는 이유는 §11-2가 미결이기 때문이다. 진입 화면은 결정 후에 그리되, 그 뒤 단계는 지금 설계해도 버려지지 않는다. (2026-08-17 phs00 결정)

### 첫 진입: 기록이 하나도 없을 때

설치하고 **첫 결과물을 보기까지 걸리는 시간**이 데이터 확보 방식(§11-2)에 따라 크게 다르다. 기술 선택으로만 다뤄지기 쉬운데 실은 제품 논거라 여기 적어둔다.

| 데이터 확보 방식 | 첫 결과물까지 | 비고 |
|---|---|---|
| HealthKit 읽기 | **즉시.** 과거 워크아웃이 이미 있다 | 개인 Apple Developer 계정 + 사용자 동의로 접근 가능. 별도 심사나 파트너십이 없다 (JiEung2 2026-08-16) |
| GPX 업로드 | 파일을 꺼내오는 만큼 | 애플 건강 앱은 개별 워크아웃 내보내기가 없어 서드파티 앱을 한 번 거쳐야 한다. **NRC는 공식 반출 경로가 아예 없어 제외한다** (JiEung2 2026-08-16) |
| 자체 트래킹 | **한 번 뛴 뒤.** 설치 당일에는 아무것도 못 만든다 | 셋 중 가장 길다 |

목표가 "올려서 우와를 듣는 것"이고 검증 기간이 추석 나흘이라면, **설치한 사람이 그 자리에서 결과물을 못 보는 것은 아프다.** 지인 영입은 옆에서 보여주며 권하는 상황일 텐데, 자체 트래킹이면 그 자리에서 보여줄 것이 없다. (2026-08-17 phs00 판단)

### 배경 선택

**배경은 두 갈래다.** (2026-08-17 phs00 결정)

- **우리가 제공하는 배경**: 서비스가 준비해 둔 영상이나 이미지를 고른다. 찍어둔 것이 없어도 결과물이 나온다.
- **그 자리에서 찍은 사진**: 카메라로 바로 찍어 배경으로 쓴다. 러닝 직후에 손이 가장 덜 간다.

`[확인 필요]` 사용자가 미리 찍어둔 영상을 고르는 경로를 남길지. JiEung2 원안은 이쪽을 전제하고 있었다.

### 내보낸 다음

**만든 결과물과 그 재료는 앱 안에 남는다.** (2026-08-17 phs00 결정)

- 같은 기록으로 **다른 프리셋을 다시 만들 수 있다.** P3(고르는 재미가 우선)이 한 번의 선택으로 끝나면 재미가 아니다.
- 지난 기록이 쌓여야 §7의 **고스트** 프리셋이 성립하고, §11-6 재방문 유인도 그 위에 올라간다.
- 결과물이 어디에도 안 남으면 다시 올 이유가 없다. 한 번 쓰고 마는 도구가 되는 가장 흔한 경로가 이것이다.

`[확인 필요]` 기록을 기기에만 둘지 서버에도 둘지는 §11-1(배포 형태)에 걸린다.

**인스타 스토리로 넘기는 마지막 한 걸음은 네이티브에서 원클릭이 된다.** 공식 API로 mp4를 스토리 배경에 자동 삽입한 편집 화면까지 띄울 수 있다. 다만 이건 네이티브 코드가 있어야 하고, 웹만으로는 "저장한 뒤 사용자가 직접 업로드"가 된다. §11-1의 판단 근거 중 하나다. (JiEung2 2026-08-16, jminkkk 2026-08-17)

## 7. 결과물의 형태

우리가 만들어 주는 물건은 **9:16 세로 클립 한 편**이다. 배경 위에 경로가 얹히고, 진행률을 따라 살아 움직인다. 거리와 페이스, 시간은 인스타 UI에 가리지 않는 위치에 함께 새겨진다.

**프리셋은 전부 하나의 렌더러다.** 차이는 "경로 위 진행률을 어떻게 칠하느냐"뿐이라, 하나를 만들면 나머지는 스타일 값이다. 개발 비용이 효과 개수에 비례하지 않는다. (JiEung2 2026-08-04)

이 성질이 P3(고르는 재미가 우선)을 감당 가능하게 만든다. 다만 **개수를 늘리는 비용은 코드가 아니라 다듬는 시간에서 나온다.** 프리셋 하나하나가 §8의 품질 기준을 통과해야 하기 때문이다.

### 목업에 구현된 5개

| 프리셋 | 표현 | 어울리는 코스 |
|---|---|---|
| **불빛 러너** | 빛이 경로를 달리고 지나온 길에 잔광이 남는다. 출발점에 닿는 순간 전체가 밝아진다 | 닫힌 고리 |
| **구간 점등** | km 단위로 구간이 하나씩 켜진다. 4km면 4번 | 도심, 복잡한 경로 |
| **드로잉** | 경로가 처음부터 그려져 나간다 | 기본값 |
| **페이스 색상** | 빠른 구간은 뜨겁게, 느린 구간은 차갑게 | 인터벌, 언덕 |
| **고도 리본** | 옆에서 본 고도가 차오른다 | 왕복, 산길 |

움직이는 형태는 [JiEung2 목업](../../ideation/JiEung2/2026-08-04-route-overlay-mockup.html)에서 볼 수 있다. 5개를 다 넣을지 줄일지는 §11-4 미결이다.

### 추가 후보 (제안, 결정 아님)

| 후보 | 표현 | 왜 |
|---|---|---|
| **줌 추적 후 전체 공개** | 진행점을 확대한 채로 따라가다가, 끝에서 줌아웃하며 완성된 경로 전체를 한 번에 드러낸다 | **실제 경로가 안 예쁘다는 최대 리스크를 정면으로 피한다.** 확대 상태에서는 선의 흐름만 보이고, 마지막 공개가 그대로 "우와" 순간이 된다. 다른 앱의 정적 이미지와 가장 크게 갈라지는 지점이기도 하다 |
| **고스트** | 같은 코스나 같은 거리의 지난 기록이 반투명 선으로 함께 달린다 | 08-11에 발산된 "이전의 나와 비교"를 그대로 구현한다. **두 번째 러닝부터 쓸 수 있어 재방문 유인(§11-6)과 맞물린다** |
| **심박 맥동** | 선의 굵기가 심박에 맞춰 맥동한다. 오르막에서 굵고 급해진다 | P1에 가장 충실하다. 지도에는 안 나타나는 몸의 상태가 그림이 된다. 다만 심박 데이터가 필요해 §11-2에 걸린다 |
| **스플릿 타이포** | 경로를 따라 km별 스플릿 시간이 텍스트로 흘러간다 | 장식이 아니라 기록 자체가 그림이 된다. 러너가 보면 바로 읽힌다. 넷 중 유일하게 텍스트 배치 코드가 따로 필요해 가장 비싸다 |

**줌 추적을 1순위로 본다.** 그리고 이건 엄밀히 말해 프리셋이 아니라 **카메라 레이어**라서, 위 다섯 개 어느 것과도 겹쳐 쓸 수 있다. 프리셋 개수를 줄이기로 하더라도 이것 하나는 따로 판단할 값어치가 있다. 나머지 셋은 §11-4를 늘리는 쪽이라 일정과 함께 봐야 한다. (2026-08-17 phs00 제안)

## 8. 렌더링 품질 기준

AI를 쓰지 않으므로 여기가 제품의 승부처다(P2). 다만 **수치는 이 문서에 두지 않는다.** 판정 방식이 사람의 눈이고, 지금 가진 수치는 합성 좌표에서 나온 값이라 실제 GPX가 들어오면 바뀐다. 문서에 박으면 결정된 것으로 읽히면서 곧 낡는다.

### 원칙

- 기록 그대로의 지글거림은 우리 결과물이 아니다. **다듬는다.**
- 단, **실제로 지나가지 않은 길이 보이면 안 된다.** 왜곡의 상한은 "이 그림을 보고 같은 길을 다시 뛸 수 있는가"다.
- **직선은 시원하게, 코너는 실제 길모퉁이만큼.** 직선을 펴려고 코너를 뭉개지 않는다.
- **없는 선을 만들어내는 조작은 기본으로 하지 않는다.** (예: 왕복 구간이 겹쳐 안 읽힐 때 두 줄로 벌리는 처리) 켤 때는 사용자가 안다.

두 번째 항목이 판정의 축이다. "예쁘게"는 반박할 수 없어서 아무것도 막지 못하지만, "같은 길을 다시 뛸 수 있는가"는 셋이 화면을 보고 바로 판정할 수 있다.

### 기준 화면

[경로 보정 수준 비교](../../ideation/phs00/2026-08-17-route-stylization-levels.html) (브라우저로 열면 조절할 수 있다)

- 합의된 설정을 URL 해시로 고정해 이 자리에 적는다. 셋이 같은 화면을 봐야 합의가 성립한다.
- **현재 제안값(팀 합의 대기)**: `#c=city&sm=55&cn=22&sep=0`
- 값을 고를 때는 시드를 바꿔가며 여러 샘플에서 확인한다. 한 샘플에만 맞는 값이면 기준이 못 된다.
- 이 화면의 트랙은 합성 좌표다. 절대적인 "예쁨"은 실제 GPX 샘플 확보 후 다시 판정한다.

### 판정

팀 3명 육안 합의 + 외부인 반응 테스트. 3주차(8/26~9/1) 퀄리티 주간의 통과 조건으로 쓴다. (2026-08-17 phs00 결정)

## 9. 기록 열람 정책

지금 코드로 만들 것은 없지만, 정책을 먼저 정해두면 나중 확장이 막히지 않는다. 문장을 쓰는 비용은 0이다.

**타인의 상세 기록을 보고 싶은 니즈는 있다.** NRC에서는 보이지 않는데, 통짜로 열면 경로까지 딸려 나가기 때문으로 보인다(추정).

그래서 **항목을 나눠서 정한다.**

| 항목 | 정책 | 이유 |
|---|---|---|
| 거리, 평균 페이스, 시간 | 함께 뛴 그룹 멤버에게 공개 | 민감도가 낮고, 비교와 경쟁 요소의 재료가 된다 |
| **경로 좌표** | 별도 동의 없이는 공개하지 않는다 | 경로는 곧 집 위치다. 다른 항목과 민감도가 다르다 |

이렇게 나눠두면 나중에 "내가 지금 뛰는 기록과 타인 기록을 비교"하는 경쟁 요소로 확장할 때, 경로를 열지 않고 숫자만으로 성립한다. (2026-08-17 phs00 결정)

## 10. 로드맵

### 그룹 드로잉 (MVP 이후 1순위)

같은 날 함께 뛴 사람들의 기록을 **하나의 드로잉 파일에 모아** 인스타에 올린다. 타인의 거리와 평균 페이스도 선택적으로 함께 올린다.

**"다음 라운드"에서 1순위로 올린다.** 이유는 이것이 동기부여 장치가 아니라 **바이럴 장치**이기 때문이다. 세 명이 같이 뛰면 세 명이 각자 올린다. "우와를 1회 이상"이라는 목표에 가장 직선으로 꽂힌다.

MVP에서 뺀 이유는 순수하게 일정이다. 친구·그룹 기능(계정, 관계, 초대, 동기화)이 선행 조건이고, 이걸 5주 안에 넣으면 3주차 퀄리티 주간이 사라진다. P2에 따라 그 주간을 깎으면 목표 자체를 잃는다.

**단, 비용이 §11-2에 크게 걸려 있다.** 데이터 확보가 GPX 업로드로 확정되면 이미 파일을 다루고 있으므로, 계정 없이 트랙을 주고받아 한 명이 합쳐 그리는 최소 형태가 가능해진다. 그 경우 MVP 말미 편입을 재검토한다. (2026-08-17 phs00 결정)

### 그 다음

코스 공유와 네비게이션, 페이스 경쟁(§9 정책 위에서), 온라인 마라톤, 의도적 드로잉(원하는 그림을 정해두고 그 모양대로 뛰기).

## 11. 미결

1~3은 1주차 조사가 끝나 **담당자 추천안이 나와 있다.** 회의에서 확정만 하면 된다. 4~6은 아직 열려 있다.

| # | 항목 | 담당 | 1주차 결과 | 확정되면 바뀌는 곳 |
|---|---|---|---|---|
| 1 | **배포 형태** | jminkkk | **추천: iOS 네이티브로 만들고, 9/22의 "출시"를 TestFlight 공개 링크로 정의한다.** App Store 정식 등재는 검증 기간(~9/29) 목표로 분리. 정식 등재를 9/22 기준으로 잡으면 리젝 1회까지만 버틴다 | §1 출시 정의, §6 진입 화면, 내보내기와 공유 경로 |
| 2 | **데이터 확보 방식** | JiEung2 | HealthKit은 개인 계정으로 접근 가능. **NRC는 반출 경로가 없어 탈락.** 안드로이드는 Health Connect 경로가 열려 있음. 셋 중 무엇을 MVP에 넣을지는 미확정 | §6 진입 화면, §10 그룹 드로잉 비용 |
| 3 | **기술 스택** | jminkkk, JiEung2 | **양쪽이 독립적으로 네이티브 셸로 수렴했다.** JiEung2는 내보내기를 AVFoundation으로, UI는 웹뷰로 두고 브릿지로 잇는 구조를 제안 | §12 |
| 4 | **MVP 프리셋 개수** (5개 전부 / 2~3개 / 1개. 추가 후보는 §7) | 팀 | §5, §7. 일정에 부담이 되면 가장 보편적인 스타일 하나로 줄여도 된다(2026-08-17 phs00) |
| 5 | **출력물이 움직이는 클립이어야 하는지, 정지 이미지도 허용인지** | 팀 | §5, §8. 배경 쪽은 §6에서 정리됐고, 여기서 남은 것은 내보내는 결과물이다. mp4가 안 될 경우의 폴백이기도 하다(§12) |
| 6 | **재방문 유인을 무엇으로 할 것인가** | phs00 | §5-4 |

### 6에 대한 판단 기준

무엇을 고르든 아래 두 질문에 답해야 한다. 이 기준을 적어두는 이유는, 기준 없이 후보만 늘어놓으면 3주차에 다시 원점에서 논의하게 되기 때문이다.

1. **다시 오게 만드는가.** 한 번 써보고 마는 도구가 되면 안 된다.
2. **공유를 유발하는가.** 혼자 뿌듯한 것으로는 부족하다. 목표가 남에게 "우와"를 듣는 것이므로, 남을 끌어들이지 못하는 장치는 이 자리에 맞지 않는다.

두 번째 기준 때문에 "결과물에 누적 거리 한 줄 새기기" 같은 후보는 약하다고 본다. 가장 강한 후보는 그룹 드로잉인데 §10의 이유로 MVP에서 빠졌다. 남은 자리를 무엇으로 채울지가 미결이다.

한 가지 실마리는 §7의 **고스트** 프리셋이다. 두 번째 러닝부터 쓸 수 있으므로 "다시 와야 볼 수 있는 것"이 생기고, 결과물이 성장을 보여주므로 공유될 이유도 함께 붙는다. 별도 기능을 만드는 대신 프리셋 하나로 두 기준을 다 건드린다.

## 12. 리스크와 폴백

미결(§11)과 리스크는 다르다. **미결은 우리가 정하면 닫히지만, 리스크는 정해도 안 닫힌다.** 그래서 각각에 판정 시점과 폴백을 붙인다.

### 제품이 성립하지 않게 만드는 것

| 리스크 | 왜 치명적인가 | 판정 시점 | 폴백 |
|---|---|---|---|
| ~~mp4 내보내기가 기술적으로 안 된다~~ → **1차 방식이 실패했고 경로를 바꾸는 중이다** | 결과물을 만들어도 인스타로 넘길 수 없다 | **1주차 판정 완료.** 실시간 캡처(`MediaRecorder`)는 배경 영상을 합성하면 무너진다. 오프라인 프레임 렌더링으로 전환 필요 (JiEung2 2026-08-16) | 전환 경로가 둘이다. 웹을 유지하면 WebCodecs, 네이티브 셸이면 AVFoundation. **둘 다 이번 주에 완전히 검증되지 않았다.** 이것이 남은 리스크다. 최후 폴백은 정지 이미지(§11-5) |
| **실제 GPX가 목업만큼 예쁘지 않다** | 렌더링이 유일한 레버(P2)인데 그 레버가 약해진다. 기준 목업의 좌표는 합성이라 아직 검증되지 않았다 | 2주차, 실제 샘플 확보 후 | 줌 추적(§7)으로 확대 구간을 주로 보여준다. 보정 강도를 §8의 상한 안에서 올린다 |

### 일정

| 리스크 | 판정 시점 | 폴백 |
|---|---|---|
| **팀에 앱 개발 경험자가 없고, 여기에 App Store 심사 기간이 붙는다** | 1주차 (jminkkk) | **9/22의 "출시"를 TestFlight 공개 링크로 정의한다**(§11-1). 심사 대기를 일정에서 들어낸다 |
| **백엔드 작업이 사실상 없다** | MVP 범위에 서버가 거의 필요 없는데 팀은 기획 1 + 백엔드 2다. 배포 형태와 무관한 인력 배치 문제다 | 1주차 (jminkkk) | 회의 안건. 이 PRD가 답할 수 있는 종류가 아니다 |

### 품질

| 리스크 | 판정 시점 | 대응 |
|---|---|---|
| **인스타 재압축으로 얇은 선이 뭉개진다** | 3주차. 실제로 올렸다가 내려받아 확인한다 | 선 굵기와 대비의 하한을 실측으로 정한다. FRD 항목이다 |
| **인코딩이 오래 걸려 기다리다 이탈한다** | 2주차 | 기다리는 화면을 설계한다. 정지 이미지를 먼저 보여주고 클립은 뒤에 완성하는 방법도 있다 |

### 지금 대응하지 않는 것

- **스토리에 러닝을 올리는 사람이 생각보다 적을 수 있다.** 타겟이 팀 3명과 그 주변인(§3)이라 이번 검증에는 영향이 적다. 일반 러너로 확장할 때 다시 본다. (JiEung2 2026-08-04이 `[미결]`로 남긴 항목)
- **스트라바가 같은 기능을 추가할 수 있다.** 방어선은 프리셋과 렌더링 완성도라고 보지만 미검증이다. 사이드 프로젝트 규모에서 지금 쓸 수 있는 수단이 없다.

## 13. 성공 판정

| 시점 | 판정 |
|---|---|
| 3주차 (8/26~9/1) | §8 품질 기준 통과. 육안 합의 + 외부인 반응 |
| 5주차 (9/9~9/15) | 팀 3명이 실제로 뛰고 실제로 스토리에 올린다 |
| 9/22 | 출시 |
| ~9/29 | **주변인에게서 "우와" 1회 이상.** 추석 연휴를 활용한다 |

역산 일정(6주): W1 조사와 결정 / W2 코어 루프 관통 / W3 퀄리티 집중 / W4 공유 흐름, 기능 추가는 여기서 끊음 / W5 실사용 / W6 버그와 배포. 출처: 2026-08-11 회의.

### "우와"를 어떻게 세나

목표가 정성적이라 **무엇을 셀지 미리 정해두지 않으면 9/29에 "들은 것 같기도 한데"로 끝난다.** (2026-08-17 phs00 결정)

**세는 것**

| 등급 | 예 |
|---|---|
| 반응 | 스토리 답장이나 대면으로 "우와", "예쁘다" |
| 질문 | "이거 뭐야", "어떻게 만든 거야" |
| 의향 | "나도 써보고 싶다", 설치 방법을 물어봄 |

**세지 않는 것**

- **좋아요와 조회수.** 평소에도 나오는 숫자라 신호가 아니다.
- **팀 3명이 서로 주는 반응.** 목표의 대상은 주변인이다(§3). 이걸 안 빼면 목표가 저절로 달성된다.

**기록 방법**

- 기간: 5주차 실사용(9/9~)부터 검증 종료(9/29)까지. 실사용 주간에 이미 올리기 시작하므로 그때부터 센다.
- 한 곳에 모은다: `docs/ops/2026-09-verification-log.md`. 올린 사람, 날짜, 반응 원문, 등급.
- **반응과 함께 조회수도 적는다.** "몇 명이 봤는데 몇 건이 왔나"가 있어야 다음 라운드에 쓸 수 있다. **반응이 없었던 것도 기록한다.**
- 지인의 메시지를 옮길 때는 **이름과 프로필을 지우고 문구만** 남긴다.

**판정**: 반응 등급 이상이 1회 이상이면 목표 달성이다. 다만 1회는 낮은 기준이라, 달성 여부보다 **왜 반응했는지와 왜 안 했는지**가 이 기록의 실제 산출물이다. 질문과 의향 등급은 다음 라운드 판단에 쓴다.

> §8의 3주차 외부인 반응 테스트와 기록 틀은 같이 쓰되 목적이 다르다. 그쪽은 렌더링이 기준을 넘었는지 보는 품질 판정이고, 이쪽은 제품 목표 판정이다.

## 14. 출처

- [2026-08-11 통합 회의 종합](https://github.com/everyware-ie/mechuri-docs/blob/main/meetings/2026-08-11/synthesis.md) (허브, private)
- [JiEung2 경로 오버레이 공유 컨셉 (2026-08-04)](../../ideation/JiEung2/2026-08-04-route-overlay-share-concept.md)
- [JiEung2 경로 오버레이 목업 (2026-08-04)](../../ideation/JiEung2/2026-08-04-route-overlay-mockup.html)
- [jminkkk 플랫폼 기술 검토 (2026-08-11)](../../ideation/jminkkk/2026-08-11-platform-tech-review.md)
- [JiEung2 데이터 확보 방식 조사 (2026-08-16)](../../ideation/JiEung2/2026-08-16-data-acquisition-healthkit-and-gpx-research.md)
- [JiEung2 mp4 내보내기 스파이크와 방향 전환 (2026-08-16)](../../ideation/JiEung2/2026-08-16-mp4-export-spike-test-and-pivot.md)
- [jminkkk App Store 출시 가능성 (2026-08-17)](../../ideation/jminkkk/2026-08-17-appstore-release-feasibility.md)
- [jminkkk 9/22 배포 형태 결론 (2026-08-17)](../../ideation/jminkkk/2026-08-17-release-mode-decision.md)
- [phs00 경로 보정 수준 비교 (2026-08-17)](../../ideation/phs00/2026-08-17-route-stylization-levels.html)
- [컨셉 후보 브레인스토밍](../../topics/concept-candidates.md)
- "2026-08-17 phs00 결정" 표기는 이 PRD를 쓰면서 확정한 항목이다. 회의를 거치지 않았으므로 다음 회의에서 확인이 필요하다.
