---
title: 러닝 기록 선택 기능정의서 (FRD)
product: running
type: frd
status: draft
updated: 2026-08-23
derives_from: ../prd/running-drawing-mvp.md
prd_sections: "§5 포함 1번, §6 첫 진입, §6 데이터 진입"
related:
  - common-rules.md
  - route-rendering.md
  - home-and-library.md
---

# 러닝 기록 선택 FRD

> **status: `draft`.** 팀 확인 후 `approved`로 올린다. **approved가 아니면 구현 근거가 아니다.**
> 근거: [PRD](../prd/running-drawing-mvp.md) · [2026-08-18 결정](../../decisions/2026-08-18-native-ios-and-mvp-scope.md)

## 0. 범위

**데이터가 들어오는 첫 화면.** 어떤 러닝으로 만들지 고른다. 코어 루프의 1단계다.

**안 담는 것**

| 항목 | 어디로 |
|---|---|
| 고른 뒤의 흐름 | [배경 선택](background-selection.md) → [결과물 편집](result-editing.md) |
| 트랙을 어떻게 그리는지 | [경로 렌더링](route-rendering.md) |
| 권한·빈 상태·실패의 공통 원칙 | [공통 규칙](common-rules.md) |
| 만든 것이 쌓이는 자리 | [홈과 보관함](home-and-library.md) |

## 1. 출처

**MVP는 HealthKit 읽기 하나다.** GPX 임포트는 여유 시다(PRD §6).

**목록은 "내 러닝 기록 목록"이지 "HealthKit 워크아웃 목록"이 아니다.** 출처는 기록의 속성 하나일 뿐이다. 나중에 GPX가 붙으면 같은 목록에 섞이고 **"가져오기" 하나가 추가되는 것으로 끝나야 한다**(PRD §6).

## 2. 무엇을 보여주나

### 2-1. 달리기만

**HealthKit의 달리기 워크아웃만 보여준다.** 걷기, 자전거, 등산은 제외한다.

`[확인 필요]` 실외 달리기만 볼지, 트레일 러닝처럼 인접한 종류도 볼지. **제안**은 경로가 있는 달리기 계열을 다 보는 것이다. 러너가 "이건 러닝인데 왜 없지"를 겪지 않게 하는 편이 낫다.

### 2-2. 경로가 없는 기록은 목록에 넣지 않는다

**워크아웃이 있어도 좌표가 함께 저장돼 있지 않을 수 있다.** 기록을 만든 앱이 거리와 시간만 쓰고 좌표를 안 쓴 경우다. 그런 기록은 **고를 수는 있는데 그릴 수가 없다.**

**고르는 시점에 걸러낸다.** 고른 뒤에 "그릴 수 없습니다"를 알리지 않는다. [공통 규칙](common-rules.md) §3-2가 정한 대로다.

> **다만 전부 걸러지면 목록이 빈다.** 그 경우 §4의 빈 상태로 가는데, **원인이 다르므로 안내도 달라야 한다.** "기록이 없다"가 아니라 "경로가 담긴 기록이 없다"이기 때문이다.

`[확인 필요]` **좌표 없는 워크아웃이 얼마나 흔한지 모른다.** 팀 3명 기기에서 확인이 필요하다(PRD §11). 흔하다면 걸러내는 것만으로는 부족하고, 왜 안 보이는지 알려주는 자리가 필요해진다.

### 2-3. 목록 항목

| 항목 | |
|---|---|
| 날짜 | 러닝한 날 |
| 거리 | |
| 시간 | |
| 페이스 | |
| **심박** | **데이터가 있을 때만** |

**모든 출처가 줄 수 있는 것으로 한정한다**(PRD §6). HealthKit에만 있는 정보에 기대면 나중에 GPX가 붙을 때 목록이 흔들린다. 표기는 [공통 규칙](common-rules.md) §7을 따른다.

**심박이 없으면 그 자리가 빠진다.** 빈 자리를 남기지 않고 나머지가 채운다. **각인에서 쓰는 규칙과 같다**([경로 렌더링](route-rendering.md) §7-0).

이렇게 두면 이득이 하나 생긴다. **목록에서 심박이 보이면 그 기록으로 만들 때 각인에 심박이 나올지 미리 안다.** 심박이 들어간 결과물을 만들고 싶으면 그런 기록을 고르면 된다.

`[확인 필요]` 항목 수가 다른 줄이 섞였을 때의 레이아웃. 디자인 시스템에서 정한다.

`[확인 필요]` 목록에 **경로 미리보기(작은 그림)** 를 넣을지. 고르는 재미가 커지지만 목록을 그리는 비용이 붙는다. 제안은 **넣지 않고 시작**하는 것이다. 성능을 보고 나중에 더한다.

### 2-4. 정렬과 범위

- **최신순.** 러닝한 날 기준이다
- **범위를 제한하지 않는다.** 과거 기록 전부를 보여준다

범위를 자르지 않는 이유는 **"예전에 좋았던 코스"를 그리고 싶은 경우**가 있기 때문이다. PRD §6이 "설치 당일에 결과물을 볼 수 있다"를 근거로 HealthKit을 고른 것도 과거 기록이 이미 있기 때문이다.

`[확인 필요]` 기록이 수백 개일 때의 목록 성능. 필요하면 나눠 불러온다.

## 3. 권한

**목록을 열려 할 때 건강 데이터 권한을 묻는다**([공통 규칙](common-rules.md) §1-2). 앱을 열자마자가 아니다.

**거부해도 앱은 알 수 없다**(공통 규칙 §1-3). 우리가 보는 것은 "기록이 안 온다"뿐이다. 그래서 §4의 빈 상태가 거부와 없음을 함께 감당한다.

## 4. 기록이 하나도 없을 때

[공통 규칙](common-rules.md) §2-1을 따른다. 셋을 담는다.

1. **무엇이 없는지**
2. **왜일 수 있는지** - 기록이 아직 없거나, 건강 데이터 접근이 허용되지 않았을 수 있다. **둘 다 적는다**
3. **다음에 할 수 있는 것** - 설정에서 권한을 확인하는 길, 그리고 한 번 뛰고 오라는 안내

### 4-1. 세 경우가 같은 화면으로 온다

| 경우 | 앱이 아는가 |
|---|---|
| 권한을 거부했다 | **모른다** |
| 기록이 정말 없다 | 모른다 |
| 기록은 있는데 **경로가 없다** | **안다** (§2-2에서 걸러냈으므로) |

**세 번째는 구분할 수 있다.** 걸러내기 전에 워크아웃이 있었는지를 알기 때문이다. 그 경우에는 **"러닝 기록은 있지만 경로가 담긴 것이 없다"** 로 다르게 안내한다. 앞의 둘과 섞으면 "기록이 없다"는 틀린 말이 된다.

## 5. 고른 다음

- 고른 기록의 **트랙을 앱이 복사해 보관한다**(PRD §6). 이후로는 원본이 삭제돼도 그릴 수 있다
- 배경 선택으로 넘어간다
- **뒤로 와도 고른 것은 그대로다**([공통 규칙](common-rules.md) §5-1)

`[확인 필요]` **그릴 수 있는 트랙의 하한**([경로 렌더링](route-rendering.md) §2에서 넘어온 항목). 좌표 수가 지나치게 적거나 거리가 너무 짧으면 그림이 되지 않는다. **제안**은 §2-2와 같은 자리에서 함께 걸러내는 것이다. 하한 수치는 실제 데이터를 보고 정한다.

## 6. 실패

| 경우 | 동작 |
|---|---|
| 목록을 불러오다 실패 | 목록 화면에 머무르고 다시 시도하는 길을 준다(F1·F3) |
| 고른 기록의 좌표를 읽다 실패 | 목록으로 돌아온다. **다른 기록을 고를 수 있다** |

불러오기가 오래 걸리면 [공통 규칙](common-rules.md) §6의 대기 규칙을 따른다.

## 7. `[확인 필요]` 모아보기

| # | 항목 | 언제 |
|---|---|---|
| 1 | 어떤 달리기 종류까지 볼지 | 구현 전 |
| 2 | **좌표 없는 워크아웃이 얼마나 흔한지** | **팀 3명 기기 확인** |
| 3 | 목록에 경로 미리보기를 넣을지 | 성능을 보고 |
| 4 | 기록이 수백 개일 때의 목록 성능 | 구현 중 |
| 5 | 그릴 수 있는 트랙의 하한 | 실제 데이터 확보 후 |

**2번이 이 문서에서 가장 중요하다.** 흔하다면 §2-2의 걸러내기만으로는 부족하고, GPX 임포트가 여유 시가 아니라 필수가 된다(PRD §6).

### 디자인 시스템으로 넘긴 것

| 항목 | FRD가 건 제약 |
|---|---|
| 심박이 있는 줄과 없는 줄이 섞였을 때의 레이아웃 | 빈 자리를 남기지 않고 나머지가 채운다(§2-3) |
