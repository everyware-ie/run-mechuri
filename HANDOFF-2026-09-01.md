# 인계 노트 (2026-09-01)

> 프로젝트 정본이 아니다. "다음 클로드"가 이어서 작업할 수 있게 이 세션 맥락만 적은 임시 노트다.
> 흡수되면 `chore:` 커밋으로 지워도 된다. **먼저 전체를 읽고 시작.** 앞선 노트(HANDOFF-2026-08-31)는
> 이 파일로 대체됐다(브랜치·PR 정보가 낡아서 삭제).

## 0. 지금 상태 (한 줄)

"3안" 야간 네온 시안을 화면에 반영하는 중. 홈·기록목록·편집·각인·배경·인코딩·완성 화면을 시안대로
다시 짰고, 경로 렌더를 react-native-svg → **react-native-skia**로 갈아끼웠다. **네이티브 재빌드가
안 돼서 폰에서 이번 세션 변경 대부분을 아직 확인 못 함** (§2).

## 1. 레포 상태 — 반드시 먼저

```
git checkout JiEung2/fix/route-animation-and-save
git pull
```

- **브랜치**: `JiEung2/fix/route-animation-and-save` — `main` + 약 25 커밋. **아직 PR 없음.**
- 마지막 커밋: `bc1a811 fix(frontend): 배경 에셋 ASCII 파일명 + 안전 영역 세로 가운데`
- pre-commit(check-docs) 통과 상태로 커밋돼 있음.
- PR 만들 때: pre-pr-checklist 훅이 참조 FRD `approved` 검사 → 본문에 참조하는 FRD는
  `run-record-selection` / `result-editing` / `background-selection` / `export-and-share` /
  `route-rendering` 전부 approved라 통과함. **캡션·장소·날짜·설정화면은 FRD에 없으니 PR 본문에서
  근거로 인용하지 말 것** (§6-3).

## 2. ⚠️ 막힌 것 — 네이티브 재빌드

이번 브랜치에서 **네이티브가 세 군데** 바뀌었다:
1. `@shopify/react-native-skia@2.6.2` (route-preview·route-thumbnail이 씀)
2. `expo-location@~57.0.14` (장소 각인 역지오코딩)
3. `RouteRendererModule.swift` — OOM 수정 + 캡션/날짜/장소 필드 + hero 레이아웃 + `safeAreaBottomRatio 0.17`

**JS 리로드로는 안 된다.** 폰에서 `RNSkiaModule could not be found` 뜨는 이유.

```bash
cd frontend
npm install
npx expo prebuild -p ios
open ios/*.xcworkspace          # Xcode ▶️ Run 으로 실제 재빌드
# 별도 터미널:
npx expo start -c              # -c = Metro 캐시 비우기 (배경 에셋 이름 바뀌어서 필요)
```

확인: `grep -i skia ios/Podfile.lock` 에 `react-native-skia` 있어야 함.

Xcode 서명은 HANDOFF-2026-08-31 §3에 있던 그대로 (jminkkk Individual 계정, `.p12`+
`.mobileprovision` 수동 발급, 자동 서명 끄고 Team `BT2KSYYPDF` / Profile `Mechuri App Dev`,
또는 빨리 볼 거면 Xcode GUI 자동 서명 + 본인 Apple ID). `npx expo run:ios`(CLI) 쓰지 말 것.

## 3. 이번(2026-08-31~09-01) 세션에 한 것

08-31 인계 노트 이후 커밋:

| 커밋 | 내용 |
|---|---|
| `9b62faf` | 기본 배경을 phs00 에셋 3장으로 교체 (아침 morning-1 / 점심 noon-3 / 저녁 evening-3) |
| `d019119` | **한 줄 문구(캡션) + 각인 시트 시안 S6 반영** — 자리(위/아래/없음) 선택기는 제외 |
| `8208cc0` | 러닝 기록 목록 시안 S3 + **각인 배치 프리셋** (간결=`row` / 크게=`hero`) |
| `bc1a811` | 배경 에셋 ASCII 파일명 + 안전 영역 세로 가운데 + 라벨 "인스타 스토리 영역" |

### 3-1. 기록 목록 (`src/app/record-selection.tsx`) — 시안 S3

- 헤더 우측 ↻ 새로고침, 상단 "APPLE 건강 · 실외 러닝" 라벨
- 행 = 자리표시 아이콘(52px, `〜`/`—`/`!`) + 제목 "8월 22일 (토) 저녁"(`formatRunTitle`) + 메타 한 줄
- **경로 썸네일 안 그림**: §5(좌표는 고른 뒤 조회)라 목록에서 전부 조회하면 비쌈 → 아이콘 자리표시
- §2-2 좌표 없음 → 점선 + 흐린 제목, §6 조회 실패 → `failedIds` accent 점선 + "다시 시도" 인라인
- 마운트 로드는 effect 안 인라인 async IIFE (refresh는 별도 useCallback) — `set-state-in-effect` 린트 회피

### 3-2. 각인 / 캡션 (`route-preview.tsx` + `edit.tsx` 각인 시트)

- 새 타입: `StampLayout = 'row' | 'hero'`, `STAMP_LAYOUTS = [{id:'row',label:'간결'},{id:'hero',label:'크게'}]`
- `StampConfig`에 `layout`, `caption`, `placeName` 추가. `StampItem`에 `'date' | 'place'` 추가
- `StampLayerSvg` (SVG 오버레이) 가 `layout`으로 분기:
  - `hero` — 왼쪽 정렬, 거리/시간/페이스 중 첫 활성 항목을 132px 큰 글씨, 위에 캡션(38px), 아래 메타 줄(30px)
  - `row` — 가운데 정렬, 캡션(위) + 항목 한 줄(28px mono)
- **장소**: `edit.tsx`에서 `Location.reverseGeocodeAsync`로 트랙 중간점 → `district/city/subregion/name` →
  `stampConfig.placeName`에 1회 채움 (사용자가 안 건드렸을 때만)
- 각인 시트 UI: "배치" 칩(간결/크게) + "넣을 것" 칩(값까지 표시: `거리 4.22km`) + "한 줄 문구" TextInput
- **자리(위/아래/없음) 선택기는 안 만듦** — 사용자가 "자리만 빼고" 지시. 위치는 미리보기에서 드래그
- Swift 렌더러(`RouteRendererModule.swift`)도 대응: `stampLayout`·`caption`·`placeName`·`runDate` 필드,
  `drawStamps` hero/row 분기, `formatStampDate`

### 3-3. 배경 에셋 ASCII 파일명 (이번 세션 마지막, `bc1a811`)

- **증상**: 기본 배경 3장이 앱에서 하나도 안 뜸. Metro 로그: `ENOENT ... .%2Fassets%2Fbackgrounds`
- **원인**: Metro 에셋 서버가 `require('../../assets/backgrounds/저녁-3-맑은노을.png')` 의 한글 경로를
  URL 인코딩(`%2F`)한 채 디코드 못 함 → 에셋 못 찾음
- **수정**: `assets/backgrounds/*.png` 10장 전부 ASCII로 `git mv` (`morning-1`,`noon-3`,`evening-3`,…),
  `default-backgrounds.ts` / `scripts/generate-backgrounds.py` 경로 갱신. 한글 설명은 주석
- **재빌드 후 확인**: 배경 화면에서 3장 스와치 뜨는지, "다음" 눌러 다음 화면 넘어가는지

### 3-4. 안전 영역 가이드 (`bc1a811`)

- 라벨 "가려지는 영역" → **"인스타 스토리 영역"** (`edit.tsx`)
- 비대칭(상단 14% / 하단 20%) → **세로 가운데(17% / 17%)**, 안전 영역 높이(66%)는 유지
- `route-preview.tsx` `SAFE_AREA_TOP_RATIO` / `SAFE_AREA_BOTTOM_RATIO` = 0.17
- Swift `drawStamps` `safeAreaBottomRatio` 도 0.17 (미리보기 ↔ 결과물 각인 위치 일치)

## 4. ▶️ 내일 이어서 할 것 (사용자 요청 순)

### 4-1. 각인 시트 규격을 HTML 시안(S6)에 맞추기  ← **최우선**

사용자: *"각인 부분 모달도 너무 이상해 html이랑 규격차이가 많이 나는거 같은데"*.
현재 각인 시트는 스크린샷만 보고 재구성한 거라 HTML 원문의 정확한 간격·타이포·구조가 안 들어감.
**레포에 3안 HTML이 저장 안 돼 있음** (`docs/ideation/JiEung2/2026-08-31-mvp-mockup-3an.md`는 텍스트
추출본만). → **사용자한테 각인 시트(S6) 부분 HTML을 다시 받아서** `edit.tsx` 각인 시트 레이아웃을
픽셀 단위로 맞출 것. 스타일 상수는 `edit.tsx` 하단 `styles` (presetChip / chipRow / chipRowWrap /
captionInput 등).

### 4-2. 재빌드하고 이번 브랜치 변경 실기기 검증

미검증 상태인 것 전부 (§2 재빌드 후):
- Skia 미리보기 렌더 + 렉
- OOM 수정 — "영상 만들기"가 끝까지 도는지
- "기기에 저장" — add-only 권한으로 성공하는지
- 캡션 / 날짜 / 장소 각인 (TS + Swift 양쪽)
- hero(크게) 레이아웃 프리셋
- 장소 역지오코딩
- 기록 목록 S3 화면
- **배경 3장 표시** (§3-3)

### 4-3. PR 만들기

이번 브랜치 25커밋 아직 PR 없음. 위 검증 끝나면 `main`으로 PR.

## 5. FRD 기준 남은 큰 구멍

- **인스타 스토리 공유 (export-and-share §3 스토리로 넘기기)** — ❌ 미착수. **MVP 필수**. 브릿지 3건 중
  마지막(Sharing to Stories). Facebook App ID 필요(`[확인 필요]`)
- **갤러리에서 배경 고르기 + 사진 권한** (background-selection §3, common-rules §1) — 슬롯만 있음
- **Swift 렌더러 글로우를 Skia 미리보기에 맞추기** — 미리보기(Skia)와 최종 mp4(CoreGraphics) 글로우가
  다름. `route-rendering.md` 어긋남 기록. 실기기 확인 후
- **설정 화면 (S9)** — approved FRD에 없음, phs00 확인 대기
- 실기기 품질: route-rendering §10(선 굵기·대비), §7-5 안전영역 실측(지금 17/17은 제안값),
  export-and-share §6-1 인코딩 소요 시간 실측

## 6. phs00 결정 대기 (스펙 없음 — CLAUDE.md 정합성 규칙상, 구현은 했으나 FRD 승격 필요)

`docs/product/features/result-editing.md` · `export-and-share.md` · `run-record-selection.md`
확인 노트의 "어긋남 기록"에 다 적어둠:
1. **한 줄 문구(캡션)** — 시안 S6/S8b에 있는데 어느 FRD에도 없음. 구현함(JiEung2 판단). PRD §11이 약하게 봄
2. **각인 "장소" · "날짜" 항목** — route-rendering §7은 거리·시간·페이스·심박 넷. 시안·구현은 6개
3. **각인 표시 모드(항상/완성 후) UI 제거** — §7-3은 모드 선택을 상정. 시안엔 없어서 UI에서 뺌(값은 유지)
4. **설정 화면 (S9)**
5. result-editing §5 "다듬기는 기본 한 축" ↔ 시안·구현은 슬라이더 2개(SMOOTH/CORNER)
6. 인코딩 실패 "다시 시도" 버튼 (S8a) — 현재는 편집으로 돌아가 재시도(§2-4 F3)

## 7. 확인 명령 (커밋 전 항상)

```bash
cd frontend
node_modules/.bin/tsc --noEmit      # ⚠️ `npx tsc`는 엉뚱한 전역 tsc@2.0.4. 로컬 바이너리로.
node_modules/.bin/eslint src/ modules/
cd ..
python3 scripts/check-docs.py
```

**현재 lint 상태**: 16 problems (12 errors, 4 warnings) — **전부 이 작업 이전부터**
(`slider.tsx` panResponder, `edit.tsx` panResponder/sheetPanResponder ×약5, `use-color-scheme.web.ts`).
새 코드는 이 수를 안 늘렸다. tsc·check-docs 통과.

`react-hooks` 플러그인이 매우 엄격 — 렌더 중 ref `.current` 읽기/쓰기, effect 안 동기 setState,
`useCallback` 본문 추적까지 전부 잡는다. 새 코드에서 effect가 setState 하면 인라인 async IIFE 패턴 써야 함.

## 8. 디자인 시스템 / 시안

`frontend/src/constants/theme.ts` 정본. 새 화면은 이 토큰만.

```
Colors.bg #0B0D10  bgCard #14181D  text #EDF1F5  textMuted #7C8894
Colors.accent #FF5A2B  accentText #0B0D10  border/borderStrong
Colors.lineWarm #FFF3EC   // 경로 드로잉 전용 — accent와 다름
Fonts: SpaceGrotesk 500/700, JetBrainsMono 500/700
Radius.pill 26 / card 22 / chip 19   Spacing xs6 sm10 md16 lg24 xl34
```

시안 텍스트 추출: `docs/ideation/JiEung2/2026-08-31-mvp-mockup-3an.md` (12화면). **원본 `.dc.html`은
폰트 1.5MB라 레포에 없음** — S6 각인 시트 픽셀 맞추려면 사용자한테 다시 받아야 함(§4-1).
경로 드로잉 팔레트는 더 오래된 `docs/ideation/JiEung2/2026-08-04-route-overlay-mockup.html` 기준.

## 9. 기타

- FRD 정본 `docs/specs/frd/` (approved만). 구현 노트 `docs/product/features/`
- git 훅: 구현 소스 편집 시 이 브랜치에 구현 노트 필요(있음), PR 생성 시 참조 FRD approved 검사
- 2026-08-25 결정: 하단 탭 안 씀, 홈 하나에 선형 루프
- 커밋 메시지 끝: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`
- 커밋·푸시는 사용자가 시킬 때만
